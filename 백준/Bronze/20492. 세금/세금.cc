#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	double n;
	cin>>n;
	
	cout<<int(n*0.78)<<" "<<int(n*0.956)<<'\n';
}