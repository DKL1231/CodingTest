#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
bool student[31] = {};
int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	int a, b, c, d;
	cin>>a>>b>>c>>d;
	cout<<a*b+c*d<<'\n';
}