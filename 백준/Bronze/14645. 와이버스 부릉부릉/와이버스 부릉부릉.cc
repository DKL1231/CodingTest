#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	int n, k; cin>>n>>k;
	while(n--){
		int a, b;
		cin>>a>>b;
	} 
	cout<<"비와이"<<'\n';
}