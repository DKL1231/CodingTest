#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	cout<<".  .   ."<<'\n';
	cout<<"|  | _ | _. _ ._ _  _"<<'\n';
	cout<<"|/\\|(/.|(_.(_)[ | )(/."<<'\n';
}